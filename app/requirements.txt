# Core dependencies
yfinance
schedule
pytz
sqlalchemy>=2.0.0
finnhub-python
python-dotenv

# API Claude (remplace ollama)
requests>=2.31.0

# Data analysis
pandas>=2.0.0
numpy>=1.24.0

# Nouvelles sources de données
feedparser>=6.0.10  # Google News RSS

# Fallback Ollama (optionnel)
ollama  # Décommenter si vous voulez un fallback local
